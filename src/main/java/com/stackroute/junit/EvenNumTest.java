package com.stackroute.junit;
//checking whether the input is even or not

public class EvenNumTest {

    public static boolean isEven(int x) //returns boolean
    {
        if (x % 2 == 0)
            return true;
        else
            return false;
    }
}
